/*
 * pins_arduino.h — baram-nrf54l-arduino
 * SPDX-License-Identifier: MIT
 *
 * 관례상 존재해야 하는 헤더다. AVR 시절 이름이고, 내용은 variant 가 준다.
 * 라이브러리들이 `#include <pins_arduino.h>` 를 조건 없이 하는 경우가 많아
 * (예: `Adafruit_ST77xx`) 없으면 **I2C 만 쓰는 스케치도 컴파일이 깨진다.**
 * Adafruit nRF52 코어도 같은 자리에 같은 내용으로 둔다.
 */
#ifndef _PINS_ARDUINO_H_
#define _PINS_ARDUINO_H_

#include "variant.h"

#endif /* _PINS_ARDUINO_H_ */
